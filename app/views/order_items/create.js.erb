<% if @order.errors.any? || @order_item.errors.any? %>
  alert("not valid.")
<% else %>
  $(".order-text").html("<%= escape_javascript(render 'layouts/order_text')%>")
  $(".product_index-<%= @order_item.product.id %>").html("<%= escape_javascript(render 'products/stock_content', builder: @product) %>")
<% end %>
window.location.reload();
